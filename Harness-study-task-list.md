# Harness 实践练习方案

## 设计思路

Harness 的 10 个能力，在天气 Agent 中已经**用到了 7 个**（上下文隔离、工具限制、Hooks、模型路由、Skill 预加载、权限模式、持久化）。但"用过"和"理解边界"是两回事。

练习不是"再搭一个系统"，而是**用对比实验亲身感受 Harness 和 Prompt 的差异**——每个练习都是"先试 prompt-only 方式，再试 harness 方式，对比结果"。

### 核心公式

```
输出质量 = f(有效上下文, 模型能力, 迭代循环)
                ↑              ↑          ↑
          Harness 构建大部分    不变     Harness 驱动
```

---

## 实验清单

### 实验 1：工具限制 — "建议" vs "强制" ✅

**体验的 Harness 能力**：确定性控制（allowedTools）
**预计耗时**：~5 min

**目的**：体验 `allowedTools` 的硬边界 vs prompt 指令的软边界

**步骤**：

1. **Prompt-only 方式**：在主 Agent（不走子代理）对话中说：
   ```
   请你假装自己是天气查询助手，你只能用 Read 和 Write 工具，
   禁止使用 WebFetch。现在帮我查上海天气。
   ```
   → 观察：模型大概率会**无视你的禁令**，尝试调用 WebFetch 或搜索

2. **Harness 方式**：说"查上海天气"，让它走 `universal-weather-agent`
   → 观察：子代理的 `allowedTools` 只有 `Read/Write/Skill`，它**物理上不可能**直接调 WebFetch，必须走 Skill

**观察点**：
- prompt 说"禁止"时模型会不会绕过？
- allowedTools 被绕过了吗？

**结论记录**：
```
实验结果：prompt 禁令在本次简单场景下未被绕过，模型坦白说"做不到"。

关键分析：
1. 本次未绕过的原因不是 Harness 在起作用，而是 prompt 在简单场景下有效：
   - 任务无法"偷偷绕过"：查天气必须发网络请求，Read/Write 物理上做不到
   - 指令清晰 + 任务步骤少：模型的 instruction following 在简单场景下够用

2. Prompt 禁令容易失效的场景（实验未覆盖，理论补充）：
   - 长对话多步骤：第 15 轮忘了第 1 轮的约束
   - 工具是"顺手"的：禁止 Bash 但任务需要跑测试，模型会"合理化"违规
   - 禁令和目标冲突："完成 X 但不要用 Y"，Y 是最佳路径时模型倾向完成目标
   - Prompt injection：读到的文件内容诱导模型忽略约束

3. 核心区别：
   Prompt 禁令 → 模型"选择"遵守 → 概率性
   allowedTools → Harness 在调用前拦截 → 确定性（不可能被绕过）
```

**应用模式**：

| 模式 | allowedTools 策略 | 适用场景 |
|------|------------------|---------|
| 最小权限 | 只给刚好需要的（如 Read/Write/Skill） | 步骤固定的任务型 Agent |
| 排除危险 | 给大部分，去掉高危的（如去掉 Bash） | 需要灵活性但防越权的研究型 Agent |
| 全开+审批 | `"*"` + `permissionMode: default` | 探索性任务，人工逐步审批 |

**决策树**：任务步骤固定？→ 最小权限 / 需要灵活但有红线？→ 排除危险 / 完全探索性？→ 全开+审批

---

### 实验 2：上下文隔离 — 一个窗口 vs 多个窗口 🔲

**体验的 Harness 能力**：上下文架构（context fork）
**预计耗时**：~15 min

**目的**：体验子代理的 context fork 对主上下文的保护

**步骤**：

1. **无隔离**：在一个对话中连续做：
   ```
   1. 帮我读一下 reports/why-harness-is-important.md，总结要点
   2. 帮我读一下 best-practice/claude-memory.md，总结要点
   3. 帮我读一下 best-practice/claude-subagents.md，总结要点
   4. 帮我读一下 best-practice/claude-commands.md，总结要点
   5. 现在用第 1 篇的要点和第 4 篇的要点做对比分析
   ```
   → 关注上下文使用率，到第 5 步时上下文会明显膨胀

2. **有隔离**：换一个新会话，对每篇文章说：
   ```
   用子代理帮我总结 reports/why-harness-is-important.md 的核心要点，只返回 5 条要点
   ```
   → 子代理的文件读取和中间推理**不进入主上下文**，只有最终 5 条要点回来

**观察点**：
- 做第 5 步对比分析时，哪种方式的主上下文更健康？
- 哪种方式模型"记得"更准确？

**结论记录**：
```
（实验后填写）
```

---

### 实验 3：Hooks 确定性 — 模型意愿 vs 系统强制 🔲

**体验的 Harness 能力**：确定性执行（PreToolUse / PostToolUse Hook）
**预计耗时**：~5 min

**目的**：体验 Hook 的"不管模型愿不愿意都会执行"特性

**步骤**：

1. **Prompt 方式**：在对话中说：
   ```
   每次你调用任何工具之前，请先说"🔊 即将调用工具"。
   每次工具返回结果后，请说"🔊 工具调用完成"。
   现在帮我查北京天气。
   ```
   → 观察：模型大概率在前几次会遵守，但忙起来就忘了，或者格式不一致

2. **Harness 方式**：说"查北京天气"，天气 Agent 已配 PreToolUse/PostToolUse Hook
   → 语音播报**每次都会触发**，不会因为"模型忙"而跳过

**观察点**：
- prompt 版本几次调用后还在遵守规则？
- Hook 版本是否 100% 触发？

**结论记录**：
```
（实验后填写）
```

---

### 实验 4：懒加载规则 — 静态 vs 动态 🔲

**体验的 Harness 能力**：动态上下文构建（rules 的 `paths:` frontmatter）
**预计耗时**：~10 min

**目的**：体验 `paths:` frontmatter 的条件加载机制

**准备工作**：创建测试规则文件 `.claude/rules/test-lazy-load.md`：

```markdown
---
paths:
  - "reports/**"
---

# Report 编辑规则（测试用）

当编辑 reports/ 目录下的文件时：
- 每个文件开头必须有一级标题
- 必须包含 "## Sources" 章节
- 如果缺少 Sources，提醒用户补充

当你加载了这条规则时，请在回复的最后加一行：`[✅ report-rule loaded]`
```

**步骤**：

1. **测试 A**：在对话中说"帮我看看 CLAUDE.md 有没有什么可以改进的"
   → 预期：**不会**触发这条规则（CLAUDE.md 不匹配 `reports/**`）

2. **测试 B**：在同一个对话中说"帮我看看 reports/why-harness-is-important.md 有没有什么可以改进的"
   → 预期：**会**触发规则，看到 `[✅ report-rule loaded]`

**观察点**：
- 同一个会话，规则是否真的根据碰的文件路径动态加载？

**结论记录**：
```
（实验后填写）
```

---

### 实验 5：模型路由 + 成本感知 🔲

**体验的 Harness 能力**：基础设施路由（model 字段）
**预计耗时**：~5 min

**目的**：体验同一个任务在不同模型下的成本和质量差异

**步骤**：

1. 说"查东京天气" → 走 `universal-weather-agent`（`model: haiku`）
   → 记录返回速度和 token 消耗

2. 临时修改 agent 配置把 `model: haiku` 改成 `model: sonnet`，再说"查东京天气"
   → 对比返回速度和 token 消耗

**观察点**：
- 对于"步骤固定、不需要深度推理"的任务，haiku 和 sonnet 的结果质量有差别吗？
- 成本差多少？
- 这就是模型路由的价值——**用对的模型做对的事**

**结论记录**：
```
（实验后填写）
```

---

## 建议执行顺序

| 顺序 | 实验 | 耗时 | 体验的 Harness 能力 | 状态 |
|------|------|------|-------------------|------|
| 1 | 实验 1（工具限制） | ~5 min | 确定性控制 | ✅ |
| 2 | 实验 3（Hooks） | ~5 min | 确定性执行 | 🔲 |
| 3 | 实验 4（懒加载） | ~10 min | 动态上下文构建 | 🔲 |
| 4 | 实验 2（上下文隔离） | ~15 min | 上下文架构 | 🔲 |
| 5 | 实验 5（模型路由） | ~5 min | 基础设施路由 | 🔲 |

**总预计耗时**：~40 min

---

## 学习收获记录

### Harness 10 能力覆盖总结

完成所有实验后填写：

| Harness 能力 | 实验覆盖 | 理解深度（1-5） | 关键洞察 |
|-------------|---------|----------------|---------|
| 工具限制（allowedTools） | 实验 1 | | |
| Hooks（确定性执行） | 实验 3 | | |
| 权限分类（auto 模式） | 已有配置 | | |
| 上下文隔离（context fork） | 实验 2 | | |
| 懒加载规则（paths:） | 实验 4 | | |
| Skill 预加载 | 已有实践 | | |
| 模块化系统提示 | 理论理解 | | |
| 模型路由 | 实验 5 | | |
| 并行执行 | Phase 5 Day 13 | | |
| 跨会话持久化 | 已有实践 | | |
